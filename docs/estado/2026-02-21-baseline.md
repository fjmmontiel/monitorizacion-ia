# Baseline global (2026-02-21)

## Contexto
- Fuente del plan original: `/Users/usuario/Downloads/d4420e7c-30f0-41df-9117-7658b34333af_plan_monitorizacion.pdf`.
- Estado del repo capturado en: `/Users/usuario/personal/monitorizacion-ia/logs/fase-ejecucion-local/01_baseline.log`.

## Resumen ejecutivo
- El repositorio está en estado no limpio con un pivot fuerte a `monitor-only`.
- El bloque principal de cambios actuales concentra:
  - Eliminación masiva de piezas legacy de frontend.
  - Consolidación de contratos monitor en frontend y backend.
  - Ajustes de orquestador backend con `caso_de_uso`.

## Riesgos detectados en baseline
- Existen artefactos críticos sin seguimiento (`untracked`) necesarios para ejecución local (configs/fixtures/tests).
- Existen artefactos de runtime Python (`__pycache__`) no ignorados globalmente.
- El toolchain raíz de frontend no está completamente operativo en este equipo (`lerna` no disponible en `.bin`).
- Los tests FE dependientes de preset corporativo (`@architecture-components/test-config`) no son ejecutables en esta instalación tal como está.

## Gaps relevantes contra PDF
- Contrato final en código: `caso_de_uso` (no `system_name`).
- Scope implementado: fase monitor end-to-end con mocks/nativo; no hardening corporativo completo.

## Evidencia rápida
- Comandos ejecutados en baseline:
  - `git status --short`
  - `git diff --stat`
  - `git log --oneline --decorate -n 20`
- Salida completa: ver log `01_baseline.log`.
